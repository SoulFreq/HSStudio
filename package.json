{
  "name": "higher-self-studio",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vercel dev",
    "start": "vercel dev",
    "lint": "echo \"Add linting tooling (ESLint, Prettier) when ready.\""
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.6.0"
  }
}
